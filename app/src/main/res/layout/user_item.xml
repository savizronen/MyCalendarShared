<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    android:id="@+id/userCardView"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="20dp"
    android:elevation="5dp"
    app:cardElevation="5dp">

    <RelativeLayout

        android:layout_width="match_parent"
        android:layout_height="75dp"
        android:background="@color/white"
        android:padding="12dp"
        tools:context=".activities.LoginActivity">


        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/user_IMG_profilePic"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_round" />

        <TextView
            android:id="@+id/user_TXT_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@+id/user_IMG_profilePic"
            android:layout_marginStart="12dp"
            android:layout_toEndOf="@+id/user_IMG_profilePic"
            android:text="userName"
            android:textColor="#000000"
            android:textSize="15sp"
            android:textStyle="bold" />


        <TextView
            android:id="@+id/user_TXT_birthday"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/user_IMG_profilePic"
            android:layout_centerHorizontal="true"
            android:layout_marginStart="4dp"
            android:layout_marginTop="-37dp"
            android:ellipsize="end"
            android:maxLines="4"
            android:textColor="#000000"
            android:textSize="20sp" />

        <ImageView
            android:id="@+id/user_IMG_checkBox"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:layout_alignEnd="@id/user_TXT_birthday"
            android:layout_alignParentTop="true"
            android:layout_marginTop="22dp"
            android:layout_marginEnd="-75dp"
            android:layout_marginRight="-80dp"
            android:contentDescription="TODO"
            android:src="@drawable/ic_baseline_check_box_outline_blank_24" />

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>